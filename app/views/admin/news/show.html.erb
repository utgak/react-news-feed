<p class="text-success"><%= notice %></p>

<div class="card mb-3">
  <div class="card-body">
    <%= render @admin_news %>
    <div class="mt-3 d-flex">
      <%= button_to "Back to news", admin_news_index_path, class: 'btn btn-secondary me-1' %>
    </div>
  </div>
</div>